[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/orchestrator/Dockerfile"

[deploy]
startCommand = "pnpm --filter @headcount/orchestrator start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
